const axios = require('axios')

const TeamOverview = () => {

    const getTeam = () => {
        axios.get(req, res)
    }

    return (
        <div className="team-overview page">
            <h2>Team Overview</h2>
        </div>
    )
}